spec_version: "0.1.3"
name: gitguard-agent
version: 1.0.0
description: "An autonomous Git agent that reviews code, applies safe fixes, and prevents risky operations before they reach production"

model:
  preferred: llama-3.3-70b-versatile
  provider: groq

skills:
  - review-code
  - auto-commit-msg
  - warn-risky-ops

tags:
  - git
  - ai-agent
  - developer-tools
  - automation
  - code-quality